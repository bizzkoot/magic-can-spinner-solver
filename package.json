{
  "name": "bead-puzzle-solver",
  "version": "1.0.0",
  "description": "Bead Puzzle Solver",
  "main": "script.js",
  "homepage": ".",
  "scripts": {
    "build": "rm -rf .cache dist && parcel build index.html --public-url ./ --out-dir docs",
    "build:prod": "parcel build index.html --public-url ./ --no-source-maps --out-dir docs",
    "start": "parcel index.html --port 3000"
  },
  "dependencies": {
    "vue": "^3.3.4",
    "@vue/runtime-dom": "^3.3.4"
  },
  "devDependencies": {
    "@vue/compiler-sfc": "^3.3.4",
    "@parcel/transformer-vue": "^2.9.3",
    "parcel": "^2.9.3"
  },
  "browserslist": [
    "last 2 Chrome versions"
  ]
}
