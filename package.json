{
  "name": "bead-puzzle-solver",
  "version": "1.0.0",
  "description": "Bead Puzzle Solver",
  "main": "script.js",
  "homepage": ".",
  "scripts": {
    "build": "parcel build index.html --public-url ./",
    "build:prod": "parcel build index.html --public-url ./ --no-source-maps",
    "start": "parcel index.html --port 3000"
  },
  "dependencies": {
    "vue": "^2.6.14"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "@vue/runtime-dom": "^3.2.0"
  },
  "browserslist": [
    "last 2 Chrome versions"
  ]
}
